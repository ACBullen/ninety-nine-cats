<!DOCTYPE html>

    <form class="CatRentalRequest" action="<%=cat_rental_requests_url%>" method="post">
      <label for="name">Name:</label>
      <br>
      <%pool = Cat.all%>
      <select id="name" name="cat_rental_request[cat_id]">
        <option disabled selected>-PICK A CAT-</option>
        <%pool.each do |cat|%>
          <option value="<%= "#{cat.id}"%>"> <%="#{cat.name}"%></option>
        <%end%>
      </select>
      <br>
      <lable for='start'>Start Date:</lable>
      <br>
      <input id='start' type="date" name="cat_rental_request[start_date]">
      <br>
      <lable for='end'>End Date</lable>
      <br>
      <input id='start' type="date" name="cat_rental_request[end_date]">
      <br>
    </br>
      <input type="submit" value="Request Rental">
    </form>
